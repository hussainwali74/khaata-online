export interface UserInterface {
    id: number,
    name: string,
    contact_no: string,
    cnic: string,
    address: string
}
export  interface  CustomerInterface {
  name: string;
  address: string;
  cnic: string;
  phoneNumber: string;
  amountDue: string;
};